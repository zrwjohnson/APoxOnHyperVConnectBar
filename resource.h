#pragma once

// The ApiHooker.dll payload is embedded into the launcher .exe as an RCDATA
// resource so the whole tool ships as a single self-contained executable.
#define IDR_APIHOOKER_DLL 101

// Application icon (exe icon, window title-bar icon, and system-tray icon).
#define IDI_APPICON 102
